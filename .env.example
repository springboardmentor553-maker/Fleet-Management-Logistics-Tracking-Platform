# Database configuration URL for FleetFlow (Template)
# Replace 'postgres' (the password placeholder) with your actual PostgreSQL password.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fleetflow_db
